# Traffic-Sign-Image-Matcher
This project implements a computer vision pipeline to identify the most similar traffic sign image from a dataset using the SIFT (Scale-Invariant Feature Transform) descriptor.
